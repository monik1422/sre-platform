# RCA — sample-api: elevated error rate + latency during fault window

| Field | Value |
|---|---|
| Service | `sample-api` |
| Severity | **SEV1** |
| Confidence | high |
| Generated | 2026-07-29 14:22:07Z |
| Evidence window | 15 min |

## Summary
`sample-api` breached both its availability and latency SLOs. 5xx error ratio
and p95 latency rose together at 14:07Z; error logs and error spans over the
same window point to failures inside `/api/work` rather than infrastructure —
consistent with a failing downstream dependency.

## Symptom
error_ratio_5m=0.29, p95=0.83s, burn_rate_1h=58.4x, targets_up=2

## Root cause
Requests to `/api/work` are failing (~30%) and slow (+~800ms) because the
handler's downstream path is returning errors and added latency. The signal
signature (simultaneous error + latency rise, no pod restarts, targets up)
indicates an application/dependency-level fault, not a crash or scheduling
problem. In this run the cause is the injected fault; in production this same
signature maps to a degraded dependency (DB/connection pool/3rd-party API).

## Evidence
- error_ratio_5m = 0.29 ratio (budget is 0.005; ~58× burn on 1h)
- latency_p95_5m = 0.83 s (objective 0.30 s)
- targets_up = 2 (both replicas healthy — not an outage/scheduling issue)
- firing alert: SampleApiErrorBudgetFastBurn (page)
- firing alert: SampleApiLatencyHigh (ticket)
- sample error log: {"level":"error","msg":"request failed (injected fault)","route":"/api/work","code":500,"trace_id":"a1b2c3..."}
- trace a1b2c3d4e5f6... process-work 842ms error=true
- kube: no PodRestart signals (rules out crash-loop)

## Contributing factors
- No retry/timeout budget on the downstream call amplifies user-visible errors.

## Remediation
1. **[immediate]** Confirm onset (14:07Z) on the RED dashboard and check whether
   a deploy or dependency change coincides.
   - _why:_ anchors the fix to a concrete change.
2. **[immediate]** If this is the demo fault, clear it (`make chaos-clear`);
   otherwise fail over / roll back the degraded dependency or last deploy.
   - _why:_ fastest restoration of the SLI.
3. **[short-term]** Add a timeout + limited retry with circuit-breaking on the
   `downstream.db-query` call.
   - _why:_ contains dependency degradation instead of passing it to users.

## Prevention (Day-2)
- SyntheticMonitor (Temporal) already probes `/healthz`; extend it to probe
  `/api/work` so functional degradation (not just liveness) triggers RCA.
- Add a per-dependency error SLI so the next occurrence localises instantly.

---
_Generated by the AI SRE agent. Confidence reflects signal quality; a human
on-call should confirm before destructive remediation._
